# ============================================================================
# FLEXT-LDIF Configuration Example
# ============================================================================
# Copy this file to .env and customize values for your environment.
# All settings are automatically loaded by FlextLdifConfig (Pydantic Settings)
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   # FlextLdif will auto-load configuration
# ============================================================================

# ----------------------------------------------------------------------------
# FLEXT Core Configuration (inherited from flext-core)
# ----------------------------------------------------------------------------
FLEXT_ENVIRONMENT=development
FLEXT_LOG_LEVEL=INFO
FLEXT_DEBUG=false
FLEXT_TRACE=false
FLEXT_MAX_WORKERS=4

# ----------------------------------------------------------------------------
# FLEXT LDIF Format Configuration
# ----------------------------------------------------------------------------
# Character encoding for LDIF files (utf-8, utf-16, latin1)
FLEXT_LDIF_ENCODING=utf-8

# Maximum line length (RFC 2849 compliance, default: 76)
FLEXT_LDIF_MAX_LINE_LENGTH=76

# Skip comment lines during parsing
FLEXT_LDIF_SKIP_COMMENTS=false

# Validate DN format during parsing
FLEXT_LDIF_VALIDATE_DN_FORMAT=true

# Enable strict LDIF validation
FLEXT_LDIF_STRICT_VALIDATION=true

# Line separator for LDIF output
FLEXT_LDIF_LINE_SEPARATOR=\n

# LDIF version string
FLEXT_LDIF_VERSION_STRING=version: 1

# ----------------------------------------------------------------------------
# FLEXT LDIF Processing Configuration
# ----------------------------------------------------------------------------
# Maximum number of entries to process
FLEXT_LDIF_MAX_ENTRIES=1000000

# Chunk size for LDIF processing
FLEXT_LDIF_CHUNK_SIZE=1000

# Batch size for LDIF processing
FLEXT_LDIF_BATCH_SIZE=100

# Maximum number of worker threads
# (Will auto-calculate based on CPU cores if not set)
# FLEXT_LDIF_MAX_WORKERS=4

# Memory limit in MB
FLEXT_LDIF_MEMORY_LIMIT_MB=512

# Enable performance optimizations
FLEXT_LDIF_ENABLE_PERFORMANCE_OPTIMIZATIONS=true

# Enable parallel processing
FLEXT_LDIF_ENABLE_PARALLEL_PROCESSING=true

# Threshold for enabling parallel processing
FLEXT_LDIF_PARALLEL_THRESHOLD=100

# Fail processing on warnings
FLEXT_LDIF_FAIL_ON_WARNINGS=false

# ----------------------------------------------------------------------------
# FLEXT LDIF Analytics Configuration
# ----------------------------------------------------------------------------
# Enable LDIF analytics collection
FLEXT_LDIF_ENABLE_ANALYTICS=true

# Cache size for LDIF analytics
FLEXT_LDIF_ANALYTICS_CACHE_SIZE=1000

# Analytics detail level (low, medium, high)
FLEXT_LDIF_ANALYTICS_DETAIL_LEVEL=medium

# Analytics sampling rate (0.0 to 1.0)
FLEXT_LDIF_ANALYTICS_SAMPLE_RATE=1.0

# Maximum entries for analytics processing
FLEXT_LDIF_ANALYTICS_MAX_ENTRIES=10000

# ----------------------------------------------------------------------------
# FLEXT LDIF Server Configuration
# ----------------------------------------------------------------------------
# Default server type for LDIF processing
# (rfc, generic, openldap, active_directory, apache_directory,
#  novell_edirectory, ibm_tivoli, oid, oud)
FLEXT_LDIF_DEFAULT_SERVER_TYPE=rfc

# Target LDAP server type
FLEXT_LDIF_SERVER_TYPE=generic

# Enable server-specific quirk handling
FLEXT_LDIF_SERVER_SPECIFIC_QUIRKS=true

# ----------------------------------------------------------------------------
# FLEXT LDIF Validation Configuration
# ----------------------------------------------------------------------------
# Validation strictness level (strict, moderate, lenient)
FLEXT_LDIF_VALIDATION_LEVEL=strict

# Enable strict RFC 2849 compliance
FLEXT_LDIF_STRICT_RFC_COMPLIANCE=true

# ----------------------------------------------------------------------------
# FLEXT LDIF Error Handling Configuration
# ----------------------------------------------------------------------------
# Error recovery mode (continue, stop, skip)
FLEXT_LDIF_ERROR_RECOVERY_MODE=continue

# ----------------------------------------------------------------------------
# FLEXT LDIF Development and Debug Configuration
# ----------------------------------------------------------------------------
# Enable debug mode
FLEXT_LDIF_DEBUG_MODE=false

# Enable verbose logging
FLEXT_LDIF_VERBOSE_LOGGING=false

# ----------------------------------------------------------------------------
# OpenLDAP Test Server Configuration (for integration tests)
# ----------------------------------------------------------------------------
# LDAP server host
LDAP_HOST=localhost

# LDAP server port
LDAP_PORT=389

# LDAP admin DN
LDAP_ADMIN_DN=cn=admin,dc=example,dc=com

# LDAP admin password (for testing only - DO NOT use in production!)
LDAP_ADMIN_PASSWORD=admin

# LDAP base DN
LDAP_BASE_DN=dc=example,dc=com

# Enable TLS/SSL
LDAP_USE_TLS=false

# LDAP connection timeout (seconds)
LDAP_TIMEOUT=30

# ----------------------------------------------------------------------------
# Environment-Specific Presets
# ----------------------------------------------------------------------------
# For development:
#   FLEXT_ENVIRONMENT=development
#   FLEXT_DEBUG=true
#   FLEXT_LDIF_STRICT_VALIDATION=false
#   FLEXT_LDIF_DEBUG_MODE=true
#   FLEXT_LDIF_VERBOSE_LOGGING=true
#
# For production:
#   FLEXT_ENVIRONMENT=production
#   FLEXT_DEBUG=false
#   FLEXT_LDIF_STRICT_VALIDATION=true
#   FLEXT_LDIF_ENABLE_PERFORMANCE_OPTIMIZATIONS=true
#   FLEXT_LDIF_MAX_WORKERS=8
#
# For testing:
#   FLEXT_ENVIRONMENT=testing
#   FLEXT_DEBUG=false
#   FLEXT_LDIF_STRICT_VALIDATION=true
#   FLEXT_LDIF_MAX_WORKERS=2
# ----------------------------------------------------------------------------
